plugins {
    id 'multiloader-loader'
    id 'net.neoforged.moddev'
}

dependencies {
    implementation(jarJar("one.pkg.velocity_rc:velocity-native:3.4.0-SNAPSHOT")) {
        exclude group: 'io.netty'
    }
    implementation(jarJar("org.yaml:snakeyaml:2.4"))
    implementation(jarJar("at.yawk.lz4:lz4-java:${lz4_version}"))
    implementation(jarJar("com.aayushatharva.brotli4j:brotli4j:${brotli4j_version}"))
    implementation(jarJar("com.github.luben:zstd-jni:${zstd_version}"))
    implementation(jarJar("one.pkg:sewlia-config:${config_api_version}")) {
        exclude group: 'org.slf4j'
    }
}

tasks.jarJar {
    enabled = true
}

neoForge {
    version = neoforge_version
    def at = project(':common').file('src/main/resources/META-INF/accesstransformer.cfg')
    if (at.exists()) {
        accessTransformers.from(at.absolutePath)
    }
    /*parchment {
        minecraftVersion = parchment_minecraft
        mappingsVersion = parchment_version
    }*/
    mods {
        "${mod_id}" {
            sourceSet sourceSets.main
        }
    }
}

sourceSets.main.resources { srcDir 'src/generated/resources' }