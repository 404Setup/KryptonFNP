[versions]
moddev = "2.0.141"

netty = "4.2.7.Final"

sewlia-config = "1.1.0-SNAPSHOT"
recastlib = "3.4.0-SNAPSHOT"
libsl = "1.0.0-alpha.5"

[plugins]
moddev = { id = "net.neoforged.moddev", version.ref = "moddev" }

[libraries]
netty-quic-classic = { group = "io.netty", name = "netty-codec-classes-quic", version.ref = "netty" }
netty-quic-native = { group = "io.netty", name = "netty-codec-native-quic", version.ref = "netty" }

pkg-sewlia-config = { group = "one.pkg", name = "sewlia-config", version.ref = "sewlia-config" }
recastlib = { group = "one.pkg.velocity_rc", name = "velocity-native", version.ref = "recastlib" }
libsl-fabric = { group = "one.pkg.libsl", name = "fabric", version.ref = "libsl" }
libsl-neoforge = { group = "one.pkg.libsl", name = "neoforge", version.ref = "libsl" }
